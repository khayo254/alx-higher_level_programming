#!/usr/bin/bash
def max_integer(my_list=[]):
    if my_list is NOne:
        return
    if len(my_list) == 0:
        return
    max = my_list[0]
    for i in my_list:
        if i > max:
            max = 1
        return max
